/**
 * Preview Entry Point
 * This file is dynamically updated by the Director Agent
 * to point to the current composition being previewed.
 */
import { CurrentComposition as CurrentComp, currentProps } from './index';

export const CurrentComposition = CurrentComp;
export { currentProps };
