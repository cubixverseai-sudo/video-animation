/**
 * 🎥 CAMERA COMPONENTS INDEX
 * Export all camera and parallax components
 */

export { 
  VirtualCamera, 
  CameraShake, 
  CAMERA_PRESETS,
  type VirtualCameraProps, 
  type CameraMove, 
  type CameraMoveType,
  type CameraShakeProps,
} from './VirtualCamera';

export { 
  ParallaxContainer, 
  Parallax, 
  DepthLayers, 
  ScrollParallax,
  type ParallaxContainerProps, 
  type ParallaxLayerConfig,
  type ParallaxProps,
  type DepthLayersProps,
  type ScrollParallaxProps,
} from './ParallaxContainer';
